{
  "name": "module4project",
  "version": "1.0.0",
  "description": "Module 4 Project",
  "main": "index.js",
  "scripts": {
    "install": "npm --prefix backend install backend",
    "dev:backend": "npm install --prefix backend start",
    "sequelize": "npm run --prefix backend sequelize",
    "sequelize-cli": "npm run --prefix backend sequelize-cli",
    "start": "npm start --prefix backend",
    "build": "npm install && npm run build --prefix backend && npm run sequelize --prefix backend db:migrate && npm run sequelize --prefix backend db:seed:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
